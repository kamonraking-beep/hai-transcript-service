{
  "name": "honorable-ai-transcript-service",
  "version": "6.0.0",
  "description": "YouTube transcript service using Innertube API. Deploy separately from the main app.",
  "main": "src/transcript.js",
  "scripts": {
    "start": "node src/transcript.js",
    "dev": "nodemon src/transcript.js",
    "deploy:railway": "railway up",
    "deploy:render": "echo 'Set start command to: node src/transcript.js'",
    "deploy:fly": "fly deploy"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "deployment_notes": {
    "recommended": "Railway.app (free tier, deploy in 2 min)",
    "alternative": "Render.com free tier",
    "also": "Fly.io, Vercel serverless, any VPS",
    "important": "Must NOT be on Google Cloud / AWS / Azure — YouTube blocks those IPs",
    "cors_note": "Add your production domain to the allowed origins array in transcript.js"
  }
}
